package common

// define common flag names used across multiple commands
const (
	// FlagProfile is the name of the CLI flag for specifying the configuration profile to use.
	FlagProfile = "profile"
)
